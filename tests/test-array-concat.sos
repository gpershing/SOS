printingarray: (arr: array int, length: int, current: int) -> void =
    if current == length
    then 0
    else 
        print(arr[curent]);
        printingarray(arr, length, current+1)

array int x = [1, 2]
array int y = [3, 4]

array int z = x@y

printingarray(z, 4, 0)
