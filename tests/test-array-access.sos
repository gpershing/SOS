array int mult_2(int i, array int arr) = 
    if i == -1
    then arr 
    //array access and assignment
    else (arr[i] = arr[i]*2; mult_2(i - 1, arr))

int printingarray(array int arr, int length, int current) =
    if current == length
    then 1
    else print(arr[curent]); printingarray(arr, length, current+1)

array a = [1,2,3]
int n = a[0]
array int b = mult_2(3, a)

int x = printingarray(a, 3, 0) //1, 2, 3
print(n) //1
print(a[0+1]) //2
x = printingarray(b, 3, 0) //2, 4, 6

