mult_2 : (i: int, arr: array int) -> array int = 
    if i == -1
    then arr
    //array access and assignment
    else 
        arr[i] = arr[i]*2; 
        mult_2(i - 1, arr)

printingarray : (array int arr, int length, int current) -> void =
    if current == length
    then void
    else 
        print(arr[curent]);
        printingarray(arr, length, current+1)

a: array int = [1,2,3]
n: int = a[0]
b: array int = mult_2(3, a)

printingarray(a, 3, 0) //1, 2, 3
print(n) //1
print(a[0+1]) //2
printingarray(b, 3, 0) //2, 4, 6
