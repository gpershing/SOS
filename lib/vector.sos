struct vector = {float x, float y}

vector scalex(vector p, float scale) =
    float x = p.x;
    float y = p.y;
    x = scale*x;
    vector ret = {x, y};
    ret

vector scaley(vector p, float scale) =
    float x = p.x;
    float y = p.y;
    y = scale*y;
    vector ret = {x, y};
    ret
